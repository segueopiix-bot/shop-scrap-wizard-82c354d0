// Suspense fallback now handles route transitions for lazy-loaded pages.
// This component is kept as a no-op to avoid touching imports elsewhere.
const PageLoader = () => null;

export default PageLoader;
